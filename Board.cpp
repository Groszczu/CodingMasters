#include "Board.hpp"
#define EMP EMPTY_CELL

const Board BOARD{
        {
                {EMP, EMP, EMP, 'A', EMP, EMP, EMP},
                {EMP, EMP, 'B', 'C', 'D', EMP, EMP},
                {EMP, 'E', EMP, 'F', 'G', 'H', EMP},
                {'I', 'J', 'K', 'L', 'M', EMP, 'N'},
                {EMP, 'O', 'P', EMP, 'Q', 'R', EMP},
                {EMP, EMP, 'S', 'T', 'U', EMP, EMP},
                {EMP, EMP, EMP, 'V', EMP, EMP, EMP}
        }
};




